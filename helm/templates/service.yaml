apiVersion: v1
kind: Service
metadata:
  name: {{ include "algo-trading.fullname" . }}
  labels:
    {{- include "algo-trading.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "algo-trading.selectorLabels" . | nindent 4 }}